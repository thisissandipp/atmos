name: example
description: An example app that showcases atmos library.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  atmos:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
